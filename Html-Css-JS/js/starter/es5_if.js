// Verifica della maggiore età
var age = parseInt(prompt("Inserisci la tua età:"));

if (age >= 18) {
	console.log("Sei maggiorenne. Puoi accedere al contenuto.");
} else {
	console.log("Sei minorenne. L'accesso al contenuto è vietato.");
}
